// Dependencies
import React,{} from "react"
// Styles

type ContainerProps = {
    children: React.ReactNode | null,
}

const ContainerProps:ContainerProps = {
    children:null,
}

const Container = ({}:ContainerProps):JSX.Element => {
    return (
        <div className="container">

        </div>
    )
}